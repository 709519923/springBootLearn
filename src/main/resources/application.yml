server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/vueblog?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapperLocations: classpath:mapper/*.xml
